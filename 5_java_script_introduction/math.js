// generate a random decimal number between 0 and 0.99
Math.random();

var decimal = Math.random();
console.log(decimal);
console.log(decimal * 10);

var rounded = Math.ceil(0.0001);
console.log(rounded)

